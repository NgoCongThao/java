package com.s2o.backend.entity;

public enum Role {
    ADMIN,      // Admin hệ thống (Dev 6)
    OWNER,      // Chủ nhà hàng (Dev 5)
    STAFF,      // Nhân viên phục vụ (Dev 3, 4)
    CHEF,       // Đầu bếp (Dev 7)
    CUSTOMER    // Khách hàng (Dev 1, 2)
}